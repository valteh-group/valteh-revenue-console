APP_NAME=Valteh Economics Dashboard
ENVIRONMENT=development
DEBUG=true
DATABASE_URL=sqlite:///valteh_economics.db
CURRENCY=MXN

# Operational-event sources (pull model). Leave a URL empty to disable that source.
# Each source exposes GET <url>/api/operational-events (cursor-paginated).
BAAS_QRO_EVENTS_URL=http://localhost:3334
BAAS_QRO_EVENTS_TOKEN=
RPP_EVENTS_URL=http://localhost:3000
RPP_EVENTS_TOKEN=
EVENTS_SYNC_PAGE_SIZE=200
